{
  "name": "typescript-playground",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "tsc -p tsconfig.base.json --showConfig > tsconfig.json",
    "build": "tsc",
    "upgrade": "npx ncu -u && npm i",
    "prestart": "npm run build",
    "start": "node dist/app/index.js",
    "pretest": "npm run build",
    "test": "node dist/tests/index.js"
  },
  "devDependencies": {
    "@tsconfig/node20": "^1.0.2",
    "@tsconfig/strictest": "^2.0.1",
    "@types/node": "^20.4.0",
    "npm-check-updates": "^16.10.13",
    "rome": "12.1.3",
    "typescript": "^5.1.6"
  }
}
